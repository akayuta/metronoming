[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "metronoming"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "numpy",
]

[project.scripts]
metronoming = "metronoming.__main__:main"
